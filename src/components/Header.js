import React from 'react';

const Header = () => {
    return (
        <header style={{ height: '80px' }}>
            {/* Espacio para la navegación fija */}
        </header>
    );
};

export default Header;